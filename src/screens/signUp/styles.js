import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  userTextWrapper: {
    marginBottom: 20,
    marginLeft: 25,
    marginTop: 40,
  },
  userText: {
    fontSize: 16,
    fontWeight: '600',
    lineHeight: 21,
  },
});
export default styles;
